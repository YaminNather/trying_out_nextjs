import React from "react";

const AnimatedTitle: React.FC = () => {
  return (
    <h1 className={`animated_title`} data-text="Virtual Assistant">Virtual Assistant</h1>
  );
};

export default AnimatedTitle;